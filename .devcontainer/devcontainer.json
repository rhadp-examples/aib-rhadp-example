{
    "image": "quay.io/rhadp/codespaces:latest",
    "remoteUser": "user",
    "customizations": {
        "vscode": {
            "settings": {
                "cmake.sourceDirectory": "${workspaceFolder}/src",
                "cmake.buildDirectory": "${workspaceFolder}/build"
            }
        }
    },
    "postCreateCommand": "mkdir -p ~/.ssh && echo '${{ secrets.BUILDER_PRIVATE_KEY }}' > ~/.ssh/id_rsa && chmod 600 ~/.ssh/id_rsa && ssh-keyscan -H localhost >> ~/.ssh/known_hosts"
}